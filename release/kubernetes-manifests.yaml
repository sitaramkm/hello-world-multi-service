
# ----------------------------------------------------------
# WARNING: This file is autogenerated. Do not manually edit.
# ----------------------------------------------------------

apiVersion: apps/v1
kind: Deployment
metadata:
  name: green
spec:
  selector:
    matchLabels:
      app: green
  template:
    metadata:
      labels:
        app: green
    spec:
      containers:
      - name: green
        image: sitaramiyer/green:v0.6
        ports:
        - containerPort: 80
        readinessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 80
        livenessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 80
        resources:
          limits:
            memory: 256Mi
            cpu: 125m
          requests:
            cpu: 50m
            memory: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: green
spec:
  type: ClusterIP
  selector:
    app: green
  ports:
  - name: green
    port: 80
    targetPort: 80

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /$1
  name: bg-ingress
spec:
#  tls:
#  - hosts:
#    - blue.<replace-with-your-domain>
#    secretName: blue.<replace-with-your-domain>
#  - hosts:
#    - green.<replace-with-your-domain>
#    secretName: green.<replace-with-your-domain>
#  - hosts:
#    - svc.<replace-with-your-domain>
#    secretName: svc.<replace-with-your-domain>

  rules:
  - host: svc.<replace-with-your-domain>
    http:
      paths:
      - path: /
        backend:
          serviceName: green
          servicePort: 80
      - path: /blue
        backend:
          serviceName: blue
          servicePort: 80
      - path: /green
        backend:
          serviceName: green
          servicePort: 80

 
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue
spec:
  selector:
    matchLabels:
      app: blue
  template:
    metadata:
      labels:
        app: blue
    spec:
      containers:
      - name: blue
        image: sitaramiyer/blue:v0.6
        ports:
        - containerPort: 80
        readinessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 80
        livenessProbe:
          periodSeconds: 5
          tcpSocket:
            port: 80
        resources:
          limits:
            memory: 256Mi
            cpu: 125m
          requests:
            cpu: 50m
            memory: 100Mi
---
apiVersion: v1
kind: Service
metadata:
  name: blue
spec:
  type: ClusterIP
  selector:
    app: blue
  ports:
  - name: blue
    port: 80
    targetPort: 80

---
